<?xml version="1.0" encoding="utf-8"?>
<!-- res/xml/network_security_config.xml -->
<network-security-config>
    <!-- Allow clear text traffic for your development server -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Add your development server IP address and any other configurations -->
        <domain includeSubdomains="true">192.168.1.56</domain>

        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>

    <!-- Add more domain-config entries if needed for other servers -->
</network-security-config>
